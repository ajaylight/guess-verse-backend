spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/guessverse
    username: postgres
    password: Lokesh@#9090

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8089

logging:
  level:
    org.springframework.web: DEBUG

logo:
  dev:
    publishable-key: pk_dg8LnMHPTIuvXpq7zmGTRQ
    logo:
      dev:
        secret-key:sk_b-w57P-0SrGALtzp9bjt1Q
